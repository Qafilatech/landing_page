# Landing page → qafila-platform API (no local backend).
# Copy to `.env` (gitignored). Must match qafila-platform WEBSITE_DOMAIN / API_DOMAIN for cookies.
VITE_API_BASE=http://localhost:3001
VITE_WEBSITE_DOMAIN=http://localhost:3000
