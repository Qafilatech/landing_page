# Landing page overrides

Overrides `MASTER.md` for `/`.

- Keep EN/AR copy and existing register form URLs.
- Audience toggle (Customer / Driver) is a segmented control, not two identical primary buttons.
- Do not invent user counts or ratings.
- Hero may use the existing video, but overlay must be brand-teal — not a generic black wash.
- Features must consume `activeButton` from the parent (navbar and section stay in sync).
- Prefer local mockups (`/Mockup.png`, `/heroSplash3.png`) over stock Unsplash trucks.
